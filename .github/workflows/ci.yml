name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  windows-test:
    runs-on: windows-2022
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"
          cache: pip
      - uses: actions/setup-dotnet@v5
        with:
          dotnet-version: "8.0.x"
      - name: Install compatible CPU runtime
        run: python -m pip install torch==2.11.0 torchaudio==2.11.0 --index-url https://download.pytorch.org/whl/cpu
      - name: Install project
        run: python -m pip install -e ".[test]"
      - name: Check dependencies
        run: python -m pip check
      - name: Run tests
        run: python -m pytest
      - name: Build WPF Overlay
        run: dotnet build overlay_cs\SystemAudioOverlay.csproj --configuration Release
      - name: Build capture helper
        run: dotnet build overlay_cs\OverlayCapture.csproj --configuration Release
      - name: Verify build outputs
        shell: powershell
        run: |
          if (-not (Test-Path overlay_cs\bin\SystemAudioOverlay.exe)) { throw "Overlay build missing" }
          if (-not (Test-Path overlay_cs\bin\OverlayCapture.exe)) { throw "Capture helper build missing" }
